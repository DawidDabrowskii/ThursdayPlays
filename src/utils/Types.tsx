export interface Player {
  id: number;
  name: string;
  position: string;
  skillRate: number;
}

export interface Players {
  id: number;
  name: string;
  position: string;
  skillRate: number;
}
[];

export interface teamsAmount {
  amount: number;
}
